<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SweetTrack" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/greenButton</item>
    </style>

    <style name="BottomNavStyle">
        <item name="android:background">@color/greenLight</item>
        <item name="itemIconTint">@color/greenButton</item>
        <item name="itemTextColor">@color/black</item>
    </style>

    <style name="Theme.SweetTrack" parent="Base.Theme.SweetTrack" />
</resources>